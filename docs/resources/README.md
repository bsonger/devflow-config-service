# Resource Docs

Repo-owned resource field docs for `devflow-config-service`.
Keep endpoint behavior in `docs/api-spec.md` and repo boundaries in `docs/architecture.md`.

## Files
- `app_config.md`
- `workload_config.md`
