apiVersion: v1
kind: Service
metadata:
  name: devflow-config-service
  namespace: devflow-staging
spec:
  selector:
    app.kubernetes.io/name: devflow-config-service
  ports:
    - name: http
      port: 8082
      targetPort: http
  type: ClusterIP
